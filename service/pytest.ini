[pytest]
# `pythonpath = .` makes the `app` package importable without installing the service. Relative to
# this directory, which is why the suite must be run from `service/` rather than from the repo root.
pythonpath = .
testpaths = tests
